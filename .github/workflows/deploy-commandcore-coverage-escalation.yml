name: Deploy CommandCore Coverage Escalation

on:
  workflow_dispatch:
  push:
    branches:
      - agent/commandcore-coverage-escalation
      - main
    paths:
      - "supabase/functions/commandcore-coverage-escalation/**"
      - ".github/workflows/deploy-commandcore-coverage-escalation.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy coverage escalation
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          test -n "$SUPABASE_ACCESS_TOKEN" || (echo "Missing SUPABASE_ACCESS_TOKEN" && exit 1)
          test -n "$SUPABASE_PROJECT_REF" || (echo "Missing SUPABASE_PROJECT_REF" && exit 1)
          supabase functions deploy commandcore-coverage-escalation --project-ref "$SUPABASE_PROJECT_REF" --no-verify-jwt

      - name: Verify coverage escalation health
        env:
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          URL="https://${SUPABASE_PROJECT_REF}.supabase.co/functions/v1/commandcore-coverage-escalation"
          for attempt in 1 2 3 4 5; do
            RESPONSE="$(curl --silent --show-error --fail "$URL" || true)"
            if echo "$RESPONSE" | grep -q '"status":"healthy"'; then
              echo "Coverage escalation health check passed."
              exit 0
            fi
            echo "Coverage escalation not ready yet (attempt $attempt/5)."
            sleep 3
          done
          echo "Coverage escalation health check failed."
          exit 1
