name: Deploy CommandCore Uncovered Work Discovery

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - agent/commandcore-uncovered-work-discovery
    paths:
      - "supabase/functions/commandcore-uncovered-work-discovery/**"
      - ".github/workflows/deploy-commandcore-uncovered-work-discovery.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy uncovered work discovery
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          test -n "$SUPABASE_ACCESS_TOKEN" || (echo "Missing SUPABASE_ACCESS_TOKEN" && exit 1)
          test -n "$SUPABASE_PROJECT_REF" || (echo "Missing SUPABASE_PROJECT_REF" && exit 1)
          supabase functions deploy commandcore-uncovered-work-discovery --project-ref "$SUPABASE_PROJECT_REF" --no-verify-jwt

      - name: Verify health
        env:
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          URL="https://${SUPABASE_PROJECT_REF}.supabase.co/functions/v1/commandcore-uncovered-work-discovery"
          for attempt in 1 2 3 4 5; do
            RESPONSE="$(curl --silent --show-error --fail "$URL" || true)"
            if echo "$RESPONSE" | grep -q '"status":"healthy"'; then
              echo "Uncovered work discovery health check passed."
              exit 0
            fi
            echo "Function not ready yet (attempt $attempt/5)."
            sleep 3
          done
          echo "Uncovered work discovery health check failed."
          exit 1
