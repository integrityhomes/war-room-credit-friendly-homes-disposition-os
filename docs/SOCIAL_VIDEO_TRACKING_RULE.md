# Social Video Tracking Rule

A social channel is not complete unless its buyer traffic is attributable back to the exact platform, campaign, and property.

Instagram uses `medium=instagram`, TikTok uses `medium=tiktok`, and YouTube Shorts uses `medium=youtube`. The campaign and property ID travel with the tracked Dwelyx link so downstream results can be compared by channel.
